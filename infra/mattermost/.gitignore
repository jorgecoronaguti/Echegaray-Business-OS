# Secretos y estado de runtime — nunca al repo.
.env
*.env
# Backups locales (se generan en runtime; no viven en git).
backup/dumps/
# Caddy: los certificados y el estado de ACME viven en el volumen Docker `caddy_data`,
# NO en el repo. No hay estado de runtime de Caddy que ignorar acá.
# Pero sí conservar la plantilla.
!.env.example
